# Runtime requirements for openwisp-provisioning (1.2.x-compatible backport).
#
# NOTE: openwisp-users / openwisp-utils are intentionally NOT pinned here.
# They are already installed in the production environment (1.2.x) and this
# package must NOT trigger an upgrade to the 1.3 line. They are listed as
# loose, ceilinged ranges only so a fresh install still resolves correctly.
django>=4.2,<5.3
djangorestframework>=3.14,<3.16
swapper>=1.3,<2.0
